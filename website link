import { useState } from "react";
import { Heart, Mail, Sparkles } from "lucide-react";
import { NoteCard } from "@/components/NoteCard";
import { NoteInput } from "@/components/NoteInput";
import { useToast } from "@/hooks/use-toast";
import heroImage from "@/assets/hero-love-notes.jpg";

interface Note {
  id: number;
  message: string;
  time: string;
  from: string;
}

const Index = () => {
  const { toast } = useToast();
  const [notes, setNotes] = useState<Note[]>([
    {
      id: 1,
      message: "Just wanted to remind you how much I love you. Can't wait to see you again! 💕",
      time: "2 hours ago",
      from: "Your Love",
    },
    {
      id: 2,
      message: "Thinking of you and smiling. Hope your day is as amazing as you are!",
      time: "5 hours ago",
      from: "Your Love",
    },
  ]);

  const handleSendNote = (message: string) => {
    const newNote: Note = {
      id: Date.now(),
      message,
      time: "Just now",
      from: "You",
    };
    setNotes([newNote, ...notes]);
    toast({
      title: "Love note sent! 💕",
      description: "Your message has been delivered with love.",
    });
  };

  return (
    <div className="min-h-screen bg-gradient-subtle">
      {/* Hero Section */}
      <section className="relative overflow-hidden">
        <div className="absolute inset-0 bg-gradient-romantic opacity-5" />
        <div className="container mx-auto px-4 py-16 md:py-24">
          <div className="grid md:grid-cols-2 gap-12 items-center">
            <div className="space-y-6 text-center md:text-left">
              <div className="inline-flex items-center gap-2 px-4 py-2 bg-primary/10 rounded-full">
                <Heart className="w-4 h-4 text-primary" fill="currentColor" />
                <span className="text-sm font-medium text-primary">For Long Distance Love</span>
              </div>
              <h1 className="text-5xl md:text-6xl font-bold text-foreground leading-tight">
                Stay Connected
                <span className="block bg-gradient-romantic bg-clip-text text-transparent">
                  One Note at a Time
                </span>
              </h1>
              <p className="text-xl text-muted-foreground max-w-xl">
                Distance means nothing when someone means everything. Share love notes throughout your day and keep your hearts close, no matter the miles between you.
              </p>
            </div>
            <div className="relative">
              <div className="absolute inset-0 bg-gradient-romantic opacity-20 blur-3xl rounded-full" />
              <img 
                src={heroImage} 
                alt="Love notes connection" 
                className="relative rounded-3xl shadow-soft w-full h-auto"
              />
            </div>
          </div>
        </div>
      </section>

      {/* Features Section */}
      <section className="py-16 bg-background/50">
        <div className="container mx-auto px-4">
          <div className="grid md:grid-cols-3 gap-8">
            <div className="text-center space-y-3 p-6 rounded-2xl hover:bg-card transition-colors">
              <div className="inline-flex items-center justify-center w-14 h-14 bg-gradient-romantic rounded-2xl shadow-soft">
                <Mail className="w-7 h-7 text-white" />
              </div>
              <h3 className="text-xl font-semibold text-foreground">Leave Sweet Messages</h3>
              <p className="text-muted-foreground">
                Send love notes anytime, anywhere. Brighten their day with your words.
              </p>
            </div>
            <div className="text-center space-y-3 p-6 rounded-2xl hover:bg-card transition-colors">
              <div className="inline-flex items-center justify-center w-14 h-14 bg-gradient-romantic rounded-2xl shadow-soft">
                <Heart className="w-7 h-7 text-white" />
              </div>
              <h3 className="text-xl font-semibold text-foreground">Feel the Love</h3>
              <p className="text-muted-foreground">
                Read heartfelt messages that remind you you're always on their mind.
              </p>
            </div>
            <div className="text-center space-y-3 p-6 rounded-2xl hover:bg-card transition-colors">
              <div className="inline-flex items-center justify-center w-14 h-14 bg-gradient-romantic rounded-2xl shadow-soft">
                <Sparkles className="w-7 h-7 text-white" />
              </div>
              <h3 className="text-xl font-semibold text-foreground">Stay Connected</h3>
              <p className="text-muted-foreground">
                Bridge the distance with meaningful moments shared throughout the day.
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Notes Section */}
      <section className="py-16">
        <div className="container mx-auto px-4 max-w-4xl">
          <div className="space-y-8">
            <div className="text-center space-y-2">
              <h2 className="text-3xl md:text-4xl font-bold text-foreground">Your Love Notes</h2>
              <p className="text-muted-foreground">Messages that keep your hearts connected</p>
            </div>

            {/* Write Note Section */}
            <div className="bg-card rounded-3xl shadow-card p-6 md:p-8 border border-border/50">
              <h3 className="text-xl font-semibold text-foreground mb-4 flex items-center gap-2">
                <Heart className="w-5 h-5 text-primary" fill="currentColor" />
                Write a Love Note
              </h3>
              <NoteInput onSendNote={handleSendNote} />
            </div>

            {/* Notes Grid */}
            <div className="space-y-4">
              <h3 className="text-xl font-semibold text-foreground">Recent Notes</h3>
              <div className="grid gap-4">
                {notes.map((note) => (
                  <NoteCard
                    key={note.id}
                    message={note.message}
                    time={note.time}
                    from={note.from}
                  />
                ))}
              </div>
            </div>
          </div>
        </div>
      </section>

      {/* Footer */}
      <footer className="py-8 border-t border-border/30">
        <div className="container mx-auto px-4 text-center">
          <p className="text-muted-foreground flex items-center justify-center gap-2">
            Made with <Heart className="w-4 h-4 text-primary" fill="currentColor" /> for couples in love
          </p>
        </div>
      </footer>
    </div>
  );
};

export default Index;
